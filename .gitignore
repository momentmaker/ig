# Nuxt + Node
node_modules/
.output/
.nuxt/
# dist (no slash) — Nuxt creates a symlink dist → .output/public; trailing slash would skip it
dist
coverage/

# Logs
*.log
.npm/
.pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/

# Environment
.env
.env.*

# Service account JSONs MUST never be committed (see spec section "Auth")
# This is belt-and-suspenders — the canonical path is ~/.config/ig-fz-ax/sa.json
*-sa.json
sa.json
service-account*.json
